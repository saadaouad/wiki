export const bearerAuthSecurity = [{ bearerAuth: [] }] as const;

export const multipartArticleDescription =
  'Accepts multipart/form-data with fields title, content, imageUrl (optional), and image file (optional).';
